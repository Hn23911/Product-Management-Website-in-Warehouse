@model IEnumerable<Category>

<ul class="list-group">
    @foreach (var category in Model)
    {
        <li class="list-group-item">
            <a href="@Url.Action("productsByCategories", "Employee", new { categoryId = category.Id })">
                @category.Name
            </a>
        </li>
    }
</ul>
